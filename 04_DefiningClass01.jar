class Point (var x: Int, var y: Int){
  def move(dx: Int, dy: Int): Unit = {
    x = x + dx
    y = y + dy
  }
  
  override def toString: String = //상위 객체인 AnyReference에 toString이라는 메소드가 있을것같다
    s"($x, $y)" //문자열을 편하게 처리해주는 interpolation기능
}

val point1 = new Point(2, 3)
println(point1.x) //2
println(point1) //print (2, 3)
                //여기서도 toString을 overriding해서 스트링으로 찍어주는 듯
                //java 기준으로 print("object name")일 경우 toString으로 변환하여 문자열로 찍어준다.
                
//Constructors 생성자
class Pointt(var x: Int = 0, var y: Int = 0) //'0' is default value

var origin = new Pointt()
var pointt1 = new Pointt(1)
println(pointt1.x) // 1


